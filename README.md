## Testing sendgird API

sendgrid provides free email service with easy integration with Email API.
This simple one pager sends out email from any email address to any valid email address.
After testing out the code, it seems the email would be going to spam box but with the correct sender email address. 